import * as React from "react";
import { UseComponent } from "reuse";
interface Props {
    elementRef?: React.Ref<Element>;
}
declare function hoist<P extends Props>(Comp: React.ComponentType<P>, Base: UseComponent<any>): UseComponent<React.ComponentType<P>>;
export default hoist;
